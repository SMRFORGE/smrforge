# Development dependencies
-r requirements.txt

# Testing
pytest>=7.0
pytest-cov>=3.0
pytest-xdist>=2.5
pytest-asyncio>=0.21.0

# Code quality
black>=22.0
flake8>=4.0
mypy>=0.950
pylint>=2.13

# Security scanning
pip-audit>=2.6.0
bandit[toml]>=1.7.5

# Documentation
sphinx>=4.5
sphinx-rtd-theme>=1.0
sphinx-autodoc-typehints>=1.18

# Jupyter
jupyter>=1.0
ipykernel>=6.0

# Optional visualization libraries (for testing visualization/mesh_3d.py)
# These are optional - tests use mocks, but having them installed reduces skips
# plotly>=5.0  # Uncomment if you want to test with real plotly
# pyvista>=0.40.0  # Uncomment if you want to test with real pyvista

# Required for units tests (also in requirements.txt; explicit here for dev)
pint>=0.20.0

# Required for UQ (also in requirements.txt)
seaborn>=0.12.0
SALib>=1.4.0

# Optional deps that unblock skipped tests / optional features
pyarrow  # parquet support (alternative: fastparquet)
Pillow   # PIL-based image support for animations/exports
imageio  # GIF export support for animations

# Optional: line-level memory profiling (see docs/development/memory-and-performance-assessment.md)
memory_profiler>=0.61