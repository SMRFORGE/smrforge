# Sphinx and documentation build dependencies for Read the Docs.
# Install these explicitly to ensure extensions are available even if
# the main package install has issues with heavy dependencies.
sphinx>=4.5
sphinx-rtd-theme>=1.0
sphinx-autodoc-typehints>=1.18
myst-parser>=2.0.0
