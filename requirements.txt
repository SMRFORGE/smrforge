# Core dependencies
numpy>=1.20.0
scipy>=1.7.0
matplotlib>=3.4.0
pandas>=1.3.0

# Performance and optimization
numba>=0.56.0

# Data storage
h5py>=3.0.0
zarr>=2.14.0

# Fast dataframes (alternative to pandas)
polars>=0.19.0

# Nuclear data handling (optional - for cross-section fetching)
# endf-parserpy (official IAEA library) - recommended for full ENDF-6 support
# endf-parserpy>=0.14.0  # Uncomment if you want endf-parserpy support
# SANDY (alternative, good for uncertainty quantification)
# sandy>=1.0.0  # Uncomment if you want SANDY support
# Note: SMRForge includes a built-in ENDF parser, so these are optional

# Validation and schemas
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Terminal formatting and rich output
rich>=13.0.0

# HTTP requests (for nuclear data downloads)
requests>=2.25.0
httpx>=0.24.0  # Async HTTP client for parallel downloads

# Machine learning (for surrogate models)
scikit-learn>=1.0.0

# Optional: Advanced plotting and 3D visualization
# plotly>=5.0  # For interactive 3D plots (install with: pip install smrforge[viz])
# pyvista>=0.40.0  # For advanced 3D visualization and VTK export (install with: pip install smrforge[viz])
# dash>=2.0  # For web-based dashboards (install with: pip install smrforge[viz])
