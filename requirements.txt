# Core dependencies
numpy>=1.20.0
scipy>=1.7.0
matplotlib>=3.4.0
pandas>=1.3.0

# Performance and optimization
numba>=0.56.0

# Data storage
h5py>=3.0.0
zarr>=2.14.0

# Fast dataframes (alternative to pandas)
polars>=0.19.0

# Nuclear data handling (at least one required for cross-section fetching)
# Option 1: OpenMC (preferred, but requires build tools - CMake, gfortran)
openmc>=0.13.0
# Option 2: SANDY (lighter alternative, pure Python, easier to install)
# sandy>=1.0.0  # Uncomment if you prefer SANDY over OpenMC

# Validation and schemas
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Terminal formatting and rich output
rich>=13.0.0

# HTTP requests (for nuclear data downloads)
requests>=2.25.0

# Machine learning (for surrogate models)
scikit-learn>=1.0.0

# Optional: Advanced plotting
# plotly>=5.0
# dash>=2.0
